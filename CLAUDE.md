# CLAUDE.md — briefing para sessões do Claude Code

Contexto para trabalhar neste repositório. Complementa o `README.md`, que descreve o que existe e como rodar; aqui está como trabalhar e o que não fazer.

Pendências de produto ficam no `ROADMAP.md`. A rodada de saneamento estrutural de agosto de 2026 — o que foi consertado, como foi provado e o que sobrou — está em [AUDITORIA.md](AUDITORIA.md), e é de lá que uma sessão de continuidade deve partir.

## O que é isto

O **jogo**, em Godot 4.7.x (GDScript, Forward+). Câmera isométrica ortográfica travada em 30°/45°, exploração em tempo real, combate por turnos 1v1 com troca livre disputado no mesmo espaço do mapa.

O **catálogo** — criaturas, habilidades, itens, números de balanceamento — vive no repositório irmão em `../avyron-bestiary`, e chega aqui via `pnpm game:export`, que grava `data/bestiary.json` **e** espelha em `models/` todo `.glb` que o bundle referencia por `modelUrl`, mais os props de bioma em `models/biomes/` (diretório inteiro — `.gltf` com texturas compartilhadas, deduplicadas na importação). Este repo é código e asset; aquele é conteúdo — inclusive a decisão de qual corpo 3D cada criatura usa.

O bestiário **roda local** desde a versão 0.104. O deploy em produção foi aposentado — ele existia para um Claude web alimentar o catálogo por HTTP, e quem escreve hoje roda na mesma máquina.

## Regras invioláveis

1. **Nenhum número de tuning mora em código.** Dano, carga, captura, preço, taxa de minério, bolsa inicial, margem do comerciante — tudo vem do bundle, que vem do banco. Se você está prestes a escrever uma constante que um designer poderia querer ajustar, ela pertence ao bestiário. As exceções legítimas são constantes de *apresentação* (velocidade de giro da câmera, amplitude de bob) e de *engenharia* (tamanho de chunk, epsilon).

   A forma geral dessa regra — quando algo é tabela do bestiário, quando é documento de design e quando é deste repositório — está em `../avyron-bestiary/CLAUDE.md`, seção **"Onde cada informação mora"**. O teste curto: o que o jogo lê em runtime é sempre tabela; do que ele não lê, o que sobreviveria a uma troca de motor é documento, e o que não sobreviveria (posição de nó, névoa, colisão, seed) é daqui. Vale consultar antes de criar sistema novo com dado novo.

2. **`data/bestiary.json` é gerado. Nunca edite à mão.** A próxima exportação sobrescreve. Número errado se corrige no bestiário e re-exporta.

3. **Depois de criar qualquer `class_name`, rode `--headless --import`.** O cache de classes globais do Godot só é atualizado na importação, e sem isso o parser não conhece a classe nova — o erro é `Identifier "X" not declared in the current scope`, que parece erro de digitação e não é.

4. **A frente de um nó é `-Z`.** Para encarar uma direção: `atan2(-dir.x, -dir.z)`. Usar `atan2(x, z)` alinha `+Z` e o corpo anda de costas — já aconteceu, e foi o teste de cena jogável que pegou.

5. **Encarar e afastar são medidas no plano.** Os corpos apoiam o Y em regras diferentes — `CreatureActor` sobe meia cápsula, `CompanionActor` fica em `GROUND_Y` com o mesh deslocado — então qualquer produto escalar ou distância entre dois deles mede o desnível junto se não achatar antes. O sintoma é um valor quase certo (`dot 0.913` em vez de `1.0`), que lê como imprecisão de giro e é geometria de outra dimensão.

## Como o código se divide

```
scripts/data/     bundle e fórmulas puras (BestiaryData, CombatMath, MiningTable, ItemEffects)
scripts/battle/   máquina de turnos (Battle, Combatant, BattleAction) — sem nós, sem sinais
scripts/world/    mapa, atores, jogador, time, inventário
scripts/ui/       telas e painéis
scripts/dev/      suítes headless e sondas
```

A separação que importa: **`data/` indexa e calcula, não decide.** `BestiaryData` só busca no bundle; `CombatMath`, `MiningTable` e `ItemEffects` só aplicam fórmula. **`BestiaryData.stats_at_level` é o funil único dos stats** — combate, time, ficha e painel da ativa passam todos por ele, e é por isso que o bônus de classe entra ali e não em `Combatant`: aplicado no combatente, a luta veria um número e a ficha mostraria outro. Quem decide é `battle/` e `world/`. Quando surgir um sistema novo com números, ele segue esse par — um índice em `BestiaryData`, uma classe de fórmula ao lado de `CombatMath`. `ItemEffects` é o exemplo mais recente disso.

Cuidado com `effectValue`: é uma coluna só servindo códigos de efeito que a interpretam de formas diferentes — `heal_percent` guarda pontos percentuais (30, 70, 100), `capture_bonus` guarda multiplicador cru (1.5, 2.5, 4.0). Ler o campo sem olhar o `effectCode` ao lado dá um número plausível e errado.

`Battle` é `RefCounted` puro de propósito: sem nós, sem sinais, sem árvore de cena. É o que torna 2.600 batalhas simuláveis em segundos na sonda de balanceamento e o que deixa a apresentação livre para consumir os eventos no ritmo que quiser.

## Testes

Dezenove suítes headless, todas em `scripts/dev/`. Rodam sem editor. O padrão:

```gdscript
extends SceneTree
func _initialize() -> void:   # monta a cena
func _process(_d) -> bool:    # roda os testes num quadro, devolve true para sair
```

**Rodar os testes no `_initialize` não funciona** para nada que dependa da árvore: um nó adicionado à raiz antes de a árvore estar viva não conta como dentro dela, e `global_position` devolve transform vazio — toda medição vira zero.

**`queue_free()` é diferido.** Teste que remove um nó e confere a remoção no mesmo quadro falha por artefato, não por bug. Confira o estado síncrono (a referência que caiu) ou use `free()` em bancadas que você controla.

**Bancada com `_process` manual precisa de `set_process(false)`.** Senão o motor também chama, e cada passo conta dobrado.

**`class_name` novo não existe para o `--script` até o projeto ser reimportado.** O Godot resolve tipo global pelo `.godot/global_script_class_cache.cfg`, que é escrito na importação — rodar headless com `--script` não re-escaneia. O sintoma engana: as suítes continuam "OK", com contagens *menores*, e os erros reais (`Could not parse global class "X"`, `Could not find type "Y" in the current scope`) saem no stderr enquanto o teste segue e reporta verde no que sobrou. Depois de criar um script com `class_name`, rode uma vez:

```powershell
& $godot --headless --editor --quit-after 40
```

Isso regenera o cache e cria o `.uid` do arquivo novo. Só então as contagens voltam ao normal.

Screenshot de verificação: crie um `scripts/dev/shot_*.gd` descartável, rode com `--resolution 1600x900`, salve em `user://`, **apague o andaime depois**. Teste headless não vê layout.

## Convenções

- **Português** em tudo que o jogador lê e em todo comentário de código. Identificadores em inglês, seguindo o Godot.
- **1 metro = 1 unidade, mas o tamanho de criatura é padronizado.** Desde 2026-09 toda criatura tem o mesmo `sizeMeters` (1,4 m desde 2026-09-06; 0,9 m nos dois primeiros dias) — não é mais derivado do tamanho paleontológico real. Antes havia uma curva de compressão logarítmica (documento `escala-das-criaturas` no bestiário) que variava o tamanho por espécie preservando ordem relativa; foi substituída por decisão de produto, e o documento guarda o histórico dos valores. Não escrever esse número em código (regra 1) — vem de `data["stats"]["sizeMeters"]`, e mudar a política de novo é PATCH no bestiário, não commit aqui.
- **O corpo vem do `modelUrl` do bundle**, resolvido por `CreatureActor.model_path` nesta ordem: `res://models/<modelUrl>` (espelhado pelo export — o corpo DEFINITIVO da criatura, 1:1, sem clipe, produzido pelo fluxo base + casca sobre o esqueleto da UAL; ver `../mestre/README.md` e `../avyron-bestiary/scripts/convert-tripo.mjs`), depois `PLACEHOLDER_PATH` (o único corpo genérico que sobra pra toda criatura sem modelo — `models/placeholders/dungeon/Imp.glb`, Quaternius CC0, retarget na UAL), e só por fim a cápsula cinza se nem isso carregar. Desde 2026-09 nenhum dos três é recolorido por elemento (ver o comentário de topo de `element_palette.gd`) — antes eram ~30 placeholders, um por família de elemento, todos removidos junto com a recoloração; o legado `res://CRT-XXX.glb` estático na raiz do projeto também saiu (Meshy sem animação, superado pelo pipeline animado). O que já é definitivo é tudo o mais — escala, máquina de estados, raio de detecção — e independe de qual fonte o corpo usou.
- **Arte de card por criatura vem de `res://cards/<código>.png`**, espelhada pelo bestiário a partir de `apps/web/public/crt-cards/` (mesmo contrato de diretório inteiro dos props de bioma — nenhum campo do bundle aponta pra ela). `CreatureInfoPanel` (card do primeiro clique) e `ActiveCreaturePanel` (card da criatura ativa) resolvem com `ResourceLoader.exists` e escondem o `TextureRect` quando falta — cobertura é parcial de propósito (21 das 60 criaturas hoje), então ausência é o caso comum, não erro. Todo `TextureRect` que mostra essa arte como miniatura de tamanho fixo precisa de `expand_mode = TextureRect.EXPAND_IGNORE_SIZE` — sem isso o tamanho MÍNIMO do controle vira o tamanho em pixels do PNG original, não o `custom_minimum_size` pedido, e um card bem maior que a miniatura infla o painel inteiro (aconteceu nos dois painéis na primeira versão: o texto ia parar fora da área visível e sobrava só uma caixa preta enorme no lugar).
- **Não existe mais cor por elemento nem por carta — removido em 2026-09.** Até então `ElementPalette` recolorizava os placeholders compartilhados por um shader (`shaders/element_palette.gdshader`, apagado junto) lendo `palette`/`cardPalette` do bundle, e usava a mesma rampa na aura do Despertar e nos efeitos de golpe/status. Saiu porque o elenco convergiu pra UM só placeholder genérico (ver abaixo) — recolorir por elemento não distingue mais nada quando toda criatura sem modelo compartilha o mesmo corpo — e porque os corpos Meshy definitivos trazem a própria arte, que não deve ser remapeada. `ElementPalette` continua existindo, mas só pra presentação: aura do Despertar e efeitos de golpe/status (swing/claw/shield/charge, pack BinbunVFX, `assets/BinbunVFX_Vol2/`) seguem funcionando, com uma rampa NEUTRA FIXA (`NEUTRAL_MID`/`NEUTRAL_SHADOW`/`NEUTRAL_HIGHLIGHT`/`NEUTRAL_AURA`) em vez de uma por elemento ou por carta. `cardPalette` não é mais lido em lugar nenhum do jogo — se ainda sair no bundle, é campo morto do lado do bestiário (nenhum problema em não exportar, mas exportar sem leitor contraria a própria regra do bestiário sobre "campo é promessa"). Ver o comentário de topo de `scripts/world/element_palette.gd`. **Uma exceção desde 2026-09-18: o efeito de BUFF sai na cor do elemento** de quem usou, lida de `elements[].palette` no bundle por `ElementPalette.element_ramp` (autoload achado por caminho; sem ele, rampa neutra). O argumento da remoção é sobre o CORPO — na partícula, que é a mesma cena pra toda criatura, a cor é a única coisa que diz de quem ela é. A rampa continua sendo conteúdo: mudar o laranja do Fogo é PATCH no bestiário, não constante aqui. O **feixe do golpe do Despertar** e o **golpe elementar** entraram no mesmo dia pela mesma regra (bullets abaixo). Golpe básico, debuff, cura, carga e aura do Despertar seguem neutros até cada um ser revisto.
- **Golpe e status têm efeito visual desde 2026-09 — antes, nada acontecia.** `Battle` continua sem nó e sem sinal (regra de sempre), mas `EncounterDirector._play_battle_effects` (ligado a `duel.rendered`, ao lado de `_sync_awakening_auras`) lê o DELTA de `battle.log_events` a cada render e chama `play_battle_effect(kind, element_code, variant_seed)` por NOME no corpo certo — dano no ALVO (swing/claw, sorteio estável pelo código da habilidade), buff em quem usou (aura `Shatter` do StatusFX Free, `assets/BinbunVFX_Vol2/StatusFX/`, na cor do elemento — ver bullet acima), debuff/cura (`shield`) e carga (`charge`) com a rampa neutra fixa. `element_code` viaja até `ElementPalette.play_battle_effect`, que hoje só o lê pro buff. Os materiais de uma cena do pack são sub-recurso COMPARTILHADO entre instâncias: quem tinge por instância duplica o `material_override` antes (`_tint_buff`), senão o segundo buff da rodada repinta o primeiro. O corte é por CONTAGEM de eventos processados, não por acumular estado, porque `rendered` avisa "mudou" sem carregar payload — a diferença de `_sync_awakening_auras`, que reespelha ESTADO e por isso é idempotente. `Battle._log` marca `"is_player"` por REFERÊNCIA (`actor == player_active()`), não por `actor.code` — um duelo espécie-contra-a-mesma-espécie teria o mesmo código dos dois lados, e é esse campo que resolve corretamente mesmo assim. **Todo nó de VFX plugado num corpo de duelo precisa de `process_mode = PROCESS_MODE_ALWAYS` explícito** — duelo pausa a árvore inteira, e um nó novo herda `PROCESS_MODE_INHERIT` do ator, que só marca o PRÓPRIO `AnimationPlayer` como `ALWAYS` (`staged_animating`/`animate_while_paused`), nunca um filho que nasce depois dele. Foi assim que o golpe saiu sem animação numa primeira rodada: nascia e travava no quadro zero, invisível, porque a forma inteira do swing/claw depende da animação abrir — o mesmo bug atinge a aura do Despertar (`attach_area_vfx`/`play_awakening_cast`), que também pode ligar em duelo. Ver `scripts/world/element_palette.gd` (`play_battle_effect`) e `scripts/world/encounter_director.gd`.
- **O resultado do turno espera a animação — desde 2026-09, o combate tinha texto e HP instantâneos com o corpo parado.** `EncounterDirector._animate_round_events` (uma corrotina) percorre o DELTA de `battle.log_events` da rodada e dá `await` em cada evento (`_animate_one_event`): dano toca `Attack` no atacante e `HitReact` no alvo, falecimento toca `Death`, e cada um espera a duração de VFX correspondente antes do próximo. `DuelScreen._resolve_round_animated` resolve a rodada em `Battle` (síncrono, sem nó) e só então dá `await` nessa sequência antes de `_render()`; `_resolving` bloqueia as teclas de ação nesse intervalo (ESC/R continuam saindo, tratadas antes da guarda). O conflito que isso resolve: `BattleStaging` força `staged_gait(0)` (→ `Idle`) em TODO quadro assentado, e sem trava ela pisaria o clipe de combate no mesmo quadro em que ele começa — por isso `BattleStaging.lock_gait(node, bool)` existe: `EncounterDirector` tranca os dois lados antes da sequência e destranca depois, exceto quem desmaiou (fica preso na pose de queda até o duelo fechar; destrancar puxaria o cadáver de volta pro `Idle`). Uma pegadinha do próprio motor, não deste código: `HitReact` (mais curto que o compasso de dano) termina sozinho ANTES do `await` acabar, e um `AnimationPlayer` não-looping que chega ao fim limpa `current_animation` pra `""` — sem reverter a pose (só para de escrever; o último quadro fica). Ler essa string depois do compasso pra provar "tocou o clipe certo" mede um acidente de duração, não o contrato — foi o que quebrou `test_battle_effects.gd` na primeira versão. `test_staging.gd` (`_test_gait_lock`) prende a trava; o despacho pro corpo certo é síncrono e direto em `test_battle_effects.gd`.
- **Golpe de curta distância investe — desde 2026-09-18 o atacante corre até o alvo, golpeia encostado e volta.** Vale para `attackVariant` `attack` (básico) e `attack2` (elementar); `attack3` (Despertar) é conjuração à distância e segue do posto. **Quem anda o corpo é a `BattleStaging`, nunca o `EncounterDirector`** — ela é a dona única da posição em duelo, e um corpo movido por fora seria lido como "perto demais" pela correção simétrica, que empurraria os DOIS pra longe no mesmo quadro. São quatro fases (`ChargePhase` `OUT` → `HOLD` → `BACK` → `TURN`): `charge(node)` parte, o corpo fica encostado em `contact_distance()` enquanto o golpe toca, `retreat()` manda voltar ao MESMO posto de onde saiu (guardado, não recalculado do `standoff` — recalcular deslocaria o encontro a cada golpe), e no posto ele dá a meia-volta pra encarar o adversário. **A meia-volta é fase da investida, não trabalho deixado pra encenação normal**: o contra-ataque começa no quadro seguinte ao retorno e investida em curso suspende a encenação normal — na primeira versão quem voltava ficava de costas o contra-ataque inteiro e apanhava pelas costas. Pelo mesmo motivo o diretor espera `TURN` acabar antes de seguir: `charge()` aceita uma investida por vez, e seguir antes faria o contra-ataque sair do posto, sem correr. Enquanto dura, a correção simétrica e o domador ficam suspensos (o posto dele é derivado da criatura; sem isso ele correria atrás dela a cada golpe). O diretor pede e espera **quadro a quadro com teto** (`_until_charge_leaves`, `CHARGE_TIMEOUT`), não pelos sinais: o duelo pode fechar no meio da corrida, e `await` em sinal de nó liberado não retoma nunca. O efeito de dano só nasce no CONTATO — disparado antes, a partícula estoura no alvo com o atacante ainda do outro lado. O clipe da corrida sai da **escada de marcha**, não de um literal: a encenação entrega `CHARGE_SPEED` ao `staged_gait` (por fora da trava de marcha, de propósito) e o degrau novo `CreatureActor.SPRINT_THRESHOLD` — usado também pela `CompanionActor` — escolhe `Sprint` em seco; submerso a investida é `Swim`, que é o que 87% do PZ-01 vê. A marcha é zerada ANTES do sinal de chegada, senão o corpo submerso golpeia na altura de nado (o "levanta ao atacar" de 2026-09-18). `Sprint` não foi asset novo: já vinha no `UAL1.glb` e em `GaitRig.LOOPED_CLIPS`, só não tinha quem o tocasse em criatura. `test_staging.gd` (`_test_charge`) prende as fases, o posto de volta, o defensor e o domador parados, e o aborto quando um corpo some.
- **O golpe elementar (`attackVariant: attack2`) acerta na cor do elemento — desde 2026-09-18.** No contato da investida o `EncounterDirector` despacha DOIS efeitos no alvo: `damage_elemental` (o mesmo swing/claw do básico, mesmo sorteio estável, só que tingido — cinza ao lado de um estouro colorido leria como dois golpes diferentes) e `impact` (o estouro do pack "Stylized Hit FX" Free, `assets/BinbunVFX_Vol2/StylizedHitFX/`). O golpe básico (`attack`) segue com o corte neutro e sem estouro — é a diferença visível entre os dois golpes de curta distância. A cor é a do elemento da HABILIDADE, não de quem usa. Das seis cenas do pack só as duas `impact` entram (`BATTLE_IMPACT_SCENES`), escolhidas olhando captura: as `hit` somem em 0,3 s sem carregar cor na câmera do jogo, as `big_impact` abrem anéis maiores que o vão do duelo. Mesmo problema de brilho do buff e do feixe, com uma armadilha a mais: cada material da cena traz a própria `emission` (2 a 6), então `_tint_impact` escreve o valor direto em cada um, e **a bola central (`ImpactSphere`) tem freio separado porque o shader dela multiplica por `emission` duas vezes (ao quadrado), é aditivo e soma as duas faces da esfera** — no valor dos outros vira um disco amarelo-claro opaco sobre o alvo, de qualquer elemento. A luz da cena (`VFXOmniLightBB`) vem na cor também e com energia reduzida: na de fábrica ela lava o chão do duelo inteiro. O pack veio SEM arquivos `.uid`; os que estão no repo foram escritos à mão com os UIDs que as cenas referenciam — sem isso o Godot gera UIDs novos na importação e toda carga de cena avisa "invalid UID, using text path". O estouro vive 1,2 s, mais que o compasso do golpe (0,8 s), de propósito: as últimas fagulhas não somem cortadas — e é isso que deixa `test_battle_effects.gd` provar o despacho depois do `await` da rodada.
- **O golpe do Despertar (`attackVariant: attack3`) dispara um feixe de energia na cor do elemento — desde 2026-09-18.** É o pack "Beam VFX" Free (`assets/BinbunVFX/beam_vfx/` — **sem** o `_Vol2` dos outros packs, porque é o caminho que a cena do pack referencia por dentro), que traz UMA cena base branca feita pra tingir. `EncounterDirector._play_attack3_sequence` dispara no `Cast` (`_dispatch_battle_beam` → `play_battle_beam` por nome no ATACANTE → `ElementPalette.play_battle_beam`), o feixe vive exatamente o compasso do `Cast`, e o alvo só toca `HitReact` depois de `BATTLE_BEAM_OPEN_TIME` — reagir no quadro do disparo lê como apanhar de um raio que ainda não chegou. **O impacto É o feixe**: o swing/claw do corpo a corpo não é mais despachado pra `attack3`. É o único efeito que liga DOIS corpos: o feixe é filho do atacante e segue um `Marker3D` (`BeamAnchor`) filho do alvo, que sai junto com o feixe por `tree_exiting` — a âncora mora em outro corpo e ficaria órfã. A altura do peito vem de cada ator (`battle_effect_height`, por nome), que soma a flutuação de nado — sem isso o feixe sairia da cintura de um corpo submerso. Três exigências do script do pack, todas pagas em `play_battle_beam`: cor e `open_amount` só DEPOIS do `add_child` (ele monta materiais e referências em `_enter_tree`; antes disso os setters escrevem em lista vazia ou estouram em nó nulo), materiais duplicados ANTES do `add_child` (sub-recurso compartilhado, como no buff), e `beam_length` igual à distância real (o script escala o feixe por dois caminhos, e com números diferentes o comprimento pisca na abertura). O feixe não tem `AnimationPlayer`: abre e fecha por `Tween` em `open_amount`, e segue o alvo em `_physics_process` — mais um motivo pro `PROCESS_MODE_ALWAYS`. **Sem glow nem tonemap no jogo, a `emission` de fábrica estoura tudo pro mesmo amarelo-claro**: além de baixar `emission`, as bolas das pontas e as faíscas de impacto têm clarão próprio (`flash_emission`) e as faíscas da origem multiplicam por 5 fixo no shader — cada um tem freio separado em `_tint_beam`, escrito direto no material porque o script do pack só sabe o mesmo `emission` pra todos.
- **Clipes de animação seguem o vocabulário normalizado** (`Idle`, `Walk`, `Run`, `Swim`, `Swim_Idle`, `Attack`, `Attack2`, `Attack3`, `HitReact`, `Death`, `Dodge`…), garantido na conversão pelo bestiário. `Swim_Idle` e `Attack3` usam o MESMO nome do vocabulário UAL (`GaitRig.LOOPED_CLIPS`) de propósito — não são invenção nova do lado de criatura, são os nomes que a biblioteca compartilhada de retarget já usa pros mesmos conceitos. É esse vocabulário, e não a montagem, que é o contrato entre corpo e jogo: foi ele que deixou o jogador trocar de corpo em 2026-09-07 sem nenhum chamador saber. O importador de glTF não marca loop em nada: `CreatureActor._prepare_animations` marca só os clipes contínuos (`LOOPED_CLIPS`) — nunca `Death`. Trocar de clipe é sempre via `_play_clip`/`_update_clip`, que silenciam quando o clipe não existe (voadores não têm `Walk`). **A escolha do clipe é sempre uma escada de marcha e meio** — `Idle` → `Walk` → `Run` por limiar de m/s, e submerso `Swim_Idle` parado → `Swim` em movimento, nunca um literal fixo no chamador: o jogador anda a 5,2 m/s, que é corrida, e foi o binário `Idle`/`Walk` que produzia o deslize. A escada existe em três lugares pelo mesmo desenho — `GaitRig.update_motion` (humanos), `CreatureActor._gait` (selvagens, patrulha e encenação) e `CompanionActor._clip_for_speed` — e todos perguntam ao `MapTerrain` se estão submersos; corpo sem `Swim_Idle` dá braçada no lugar, corpo sem `Swim` cai na escada seca. Os catorze `.glb` do PZ-01 têm o conjunto inteiro desde 2026-09-07 — onze chegaram do Meshy só com `Walk`/`Run` e receberam os clipes do CRT-005 via `pnpm models:transfer` no bestiário (retarget relativo ao repouso, mesmo esqueleto Meshy de 24 ossos); clipe que falte num corpo novo é problema de asset, nunca de código.
- **`AnimationPlayer` é pausável.** Corpo movido com a árvore parada precisa de `PROCESS_MODE_ALWAYS` no player dele, senão o clipe fica *selecionado e congelado no quadro zero* — o corpo atravessa a cena numa pose estática e o defeito lê como "escolheram o clipe errado", que é a pista errada. Quem liga é a `BattleStaging` (`staged_animating`), só enquanto ela é dona do corpo: ligado sempre, uma criatura pega no meio do `Walk` por uma tela de loja andaria no lugar em vez de ficar parada.
- **Bioma é consulta por posição, não declaração.** `MapBiomes.biome_at(pos)` responde a partir de `maps[].biomeRegions` no bundle, avaliando as regiões na ordem do catálogo e parando na primeira que contém o ponto. Duas coisas não são óbvias e custam caro se esquecidas. **As coordenadas são normalizadas em ±1 sobre o meio-lado do mapa**, e quem divide é o Godot — é isso que faz a partição sobreviver a redimensionar o terreno sem reautorar dado, e é isso que quebra se alguém mudar uma fronteira de relevo (`COAST_RAMP_START` e companhia) sem mexer na região correspondente: a mineração passa a responder pelo lugar errado, em silêncio. `test_data.gd` VARRE o eixo medindo onde o bioma troca, justamente para essa divergência não passar. E o `DEFAULT_BIOME` do `WorldRoot` **não é mais o bioma do mapa** — virou fallback para dado faltando (mapa sem partição, ponto fora de todas as regiões); tratá-lo como "o bioma" refaz o furo que a rodada fechou.

- **Bioma decide mineração e a FREQUÊNCIA de spawn selvagem** (desde 2026-09-06). `CreatureSpawner` consulta `MapBiomes.biome_at()` na posição do jogador e lê `biomes.spawnChance` do bundle: é o bioma que decide se uma rolagem vira criatura, e é assim que "bioma sem fauna" existe sem código novo — a costa do PZ-01 tem chance 0,00. O que **continua** por predicado geográfico em código é o KEEP-OUT de posição (`on_coast`/`on_island`, que impedem o corpo de nascer em terra seca) mais ambiência e névoa. Ou seja: a partição responde *com que frequência*, os predicados respondem *onde não pode*. As duas perguntas são diferentes e ter respostas diferentes é de propósito — misturá-las traria de volta a escolha que este bullet já descreveu como custosa (um predicado novo a cada bioma).

- **O PZ-01 é o leito de um mar, e nadar é o estado normal.** 87,3% do mapa responde "submerso" (remedido depois de a costa virar lobo: ela caiu de faixa cheia para 14,1% do mapa, e o que era praia nos cantos virou mar); o seco são dois trechos declarados — o platô da costa e a **ilha da arena**, no meio do mapa, onde o jogo abre. Quem responde é `MapTerrain.submerged` — **fora desses dois é sempre submerso**, independentemente da altura (recife não é ilhota), e só neles a cota decide, porque é neles que a rampa atravessa a superfície. Trecho seco novo é geografia declarada (um predicado como `on_coast`/`on_island`), nunca um afrouxamento da regra de altura. A cota vem de `MapDressing.water_line` e é a MESMA que fragmenta a névoa: separar as duas faria a imagem contradizer o corpo.
- **Corpo movido de fora implementa o contrato de encenação.** Na abertura do duelo o mundo está pausado e nenhum dos três corpos processa: quem os leva aos postos é `BattleStaging`, e ela precisa de três coisas de cada um — `staged_ground_offset()` (quanto a origem fica acima do chão, porque as três regras divergem), `staged_gait(speed)` (a marcha imposta, para o corpo escolher o clipe pela mesma escada de sempre) e `staged_animating(bool)` (deixar o `AnimationPlayer` rodar com a árvore parada). Os três são chamados por **nome** (`Node.call`), não por tipo, para a bancada de `Node3D` solto da suíte não ser obrigada a implementá-los. Corpo novo que a encenação venha a mover entra por aí; sem os métodos ele volta a deslizar sem apoio, em silêncio.

- **Humano é UM corpo (`CharacterRig`) e UMA máquina de marcha (`GaitRig`).** Jogador e NPCs saem do kit de personagens (`models/characters/`, espelhado pelo bestiário): corpos, cabelos e peças de outfit rigados no mesmo esqueleto de 65 ossos, montados em runtime a partir de uma *receita* de nomes de peça. A do jogador é `PlayerController.PLAYER_RECIPE`, fixa no jogo; as dos NPCs vêm do bundle (`merchants[].appearance` / `duelists[].appearance` — conteúdo do catálogo, tabela `npc_appearances`). Posto do Relicário e bancada NÃO são NPC de catálogo (ponto de código, sem `npc_role`), então a pessoa diante deles veste uma receita fixa no ator (`RelicStationActor.NPC_RECIPE`/`CraftingBenchActor.NPC_RECIPE`, desde 2026-09-20) — migra para `npc_appearances` no dia em que virarem NPC de verdade. **O importador glTF do Godot corta o sufixo `_Loop` do nome do clipe** (é a convenção dele para marcar loop): `Idle_FoldArms_Loop` no arquivo da UAL2 vive como `Idle_FoldArms` na biblioteca fundida — `GaitRig.LOOPED_CLIPS` e qualquer chamador usam o nome importado, não o do manifest; medido por sonda em 2026-09-20 depois de o clipe "não existir". Entre 2026-09-07 e 17 o jogador teve um `.glb` próprio do Meshy (`PlayerRig`); voltou ao kit em 2026-09-17 para não sustentar um segundo pipeline de asset por um corpo só — o `.glb` está em `../shared-assets/legacy/meshy-player/`. Receita vazia ou peça que não carrega cai na cápsula. Se a tela de criação de personagem entrar, ela escolhe peças do kit e o escolhido persiste no save — nunca no bestiário.

  **O contrato entre corpo e jogo é o vocabulário de clipes, não a montagem** — foi por isso que o jogador trocou de corpo duas vezes (kit → Meshy → kit) sem mexer em nenhum chamador. `GaitRig` é o dono único da escada de marcha (`update_motion`), do contrato de loop (`LOOPED_CLIPS`), do laço sintético de mineração e da flutuação do nado; `swim_lift`/`swim_idle_lift` são medida do CLIPE e não do sistema (o `Swim` da UAL deita o nadador em torno da origem e precisa de 0,9 m de levantamento, e o `Swim_Idle` dela pende ainda mais fundo e precisa de 1,78 m), por isso são campo de instância e não constante.

- **Clipe do jogo é in-place, sem exceção.** Quem move um corpo é sempre o código — `CharacterBody3D` no jogador, a posição do ator nas criaturas. Um clipe que também anda faz o corpo viajar duas vezes: a malha escapa da cápsula durante o ciclo e volta de um salto quando ele reinicia. Nem a UAL é 100% assim (o `Attack3` desloca o quadril e termina lá), então `CharacterRig._build_library` tira a viagem líquida do `pelvis` de cada clipe na montagem da biblioteca — para humanos e criaturas, que dividem a biblioteca — e `test_characters.gd` e `test_creature_bodies.gd` medem que cada clipe fecha onde abriu. A correção é UMA, na montagem; compensar em cada chamador deixaria o próximo corpo repetir o defeito.
- **O set do jogador tem três peças, e só uma tem tabela própria.** O Relicário fica em `relics`/`relic_stats`/`relic_rules` porque as colunas dele são de captura ponta a ponta; Amplificador e Encantador ficam em `equipment`/`equipment_stats`/`equipment_recipes` porque são passivos que *tieram* em vez de subir de nível. Não existe `equipment_rules`, de propósito: o único número global do sistema é o clamp acumulado, que é de `Battle` e vale para toda fonte de modificador — tabela sem consumidor é a lição da migration `0014`, e ela não se repete. **O alvo do modificador sai do `slot`, nunca do `effectCode`** (`amplifier` no time do jogador, `enchanter` no adversário); os dois codificarem o alvo deixaria os dois discordarem.

- **Modificador de combate tem um ponto de aplicação só: `Battle._apply_modifier`.** Habilidade de suporte e equipamento passam por ele, e é ele que aplica `MODIFIER_MIN/MAX`. Aplicar `attack_modifier` direto de qualquer outro lugar — do loadout, de uma tela — dá ao teto acumulado um segundo dono, e a soma passa a estourar por um caminho e não pelo outro. É por isso que `PlayerLoadout.modifiers()` devolve dado cru em vez de mexer no `Combatant`.

- **Progressão na tela passa por um funil só.** `PlayerRoster.progress_at`/`storage_progress_at` e `PlayerRelic.progress` devolvem `{level, xp, xp_to_next, at_cap, xp_full, material, material_cost}`, e `ProgressText` (`scripts/ui/progress_text.gd`) transforma isso na única frase que janela do time, painel da ativa, set, posto e mensagem pós-luta usam. `xp_full` ali é o **mesmo** critério que `grant_xp_at` usa para tentar subir — uma tela que refizesse a conta ("xp >= curva") acabaria chamando de "pronta" algo que o gate não sobe, ou o contrário. Tela nova que mostre nível lê o dicionário; não recalcula.

- **Posse é o que a bancada tem e o posto do Relicário não.** `PlayerLoadout` separa fabricado (`_owned`) de vestido (`_equipped`), e `equip()` recusa o que o jogador não fabricou. O `RelicStationScreen` deixa vestir qualquer modelo do catálogo porque lá não existe sistema de aquisição — o furo está no ROADMAP e não deve ser copiado para peça nova. Quem for desenhar aquisição de relicário tem em `PlayerLoadout` a forma já provada.

- **Minério exclusivo é ausência de linha, não regra de código.** Os três minérios glaciais (`ITM-024/025/026`) só têm taxa de bioma em `BIO-014`, e `MiningTable._weight_of` lê "bioma presente sem este minério" como peso zero. Isso colide de frente com a regra de `mineracao` de que **todo bioma precisa do conjunto completo de taxas** — a regra vale para os minérios comuns, e cumpri-la literalmente para um exclusivo o torna minerável em toda parte, sem erro em lugar nenhum. `test_equipment.gd` varre os catorze biomas para acusar. O lado da **classe**, esse sim, é obrigatório nas cinco: sem ele o produto dá zero e o minério some até de onde deveria sair.

- **Toda tela é modal-que-pausa ou janela-de-HUD — não existe terceira.** Overlay modal (duelo, loja, posto do Relicário, bancada) entra como `CanvasLayer` em `PROCESS_MODE_ALWAYS`, faz `get_tree().paused = true` e entra em `WorldRoot._modal_open()`; nó pausado não recebe input, então é o pause que impede `F` de minerar no meio de uma negociação. Janela de HUD (time `T`, set `E`, bolsa `V`) **não** pausa — o mundo segue vivo atrás, e ela se guarda sozinha, como `_roster_open()`. Escolher a família errada é o jeito de vazar input: uma tela que não pausa e não tem guarda deixa o mapa responder por baixo dela. `_modal_open()` é o ponto único — tela modal nova entra lá, e `test_merchant.gd` (`_test_modal_guard`) prende o invariante.
- **Ponto fixo clicável herda de `InteractableActor`.** Comerciante, arena, posto do Relicário, bancada e guardião do portal são a mesma coisa mecanicamente: `StaticBody3D` (o clique é raycast físico), alcance de interação, placa flutuante, apoio no chão, e um sinal `engaged` que não conhece tela nenhuma. A subclasse traz só o que a distingue — corpo, estado próprio e a silhueta da placa. Os três serviços da vila da costa são, desde 2026-09-20, estrutura (`.glb`, cenário) + pessoa (`attach_npc`, um `CharacterRig` filho do MESMO ator, diante da fachada, com placa sobre a cabeça e cápsula de clique própria) — nunca um segundo ator para a pessoa, senão haveria dois pontos de interação para manter em sincronia. `CreatureActor` **não** entra: ela responde por seleção + segundo clique, contrato diferente. `WorldRoot.handle_click_at` e `WorldSelection.pick_body` testam contra o tipo base, então ator novo não exige edição em nenhum dos dois — antes eram duas listas de classes concretas mantidas à mão, e esquecer a segunda fazia o clique não fazer nada, sem erro.
- **Quem apoia corpo no chão usa `ground_on_spot()`, nunca `position.y = ...`.** O `y` do spot é a altura do terreno naquele ponto — zero no centro plano, mas não na costa —, e a origem do ator é o centro do corpo. Somar meia altura funciona nos dois casos; atribuir só funciona em chão plano e enterra o ator assim que ele se move para terreno elevado. Os atores já divergiram nisso (dois somavam, dois atribuíam) e estavam certos por coincidência de posição; `test_merchant.gd` (`_test_actor_grounding`) prende a regra.
- **Bioma sem `mining_rates` é a armadilha silenciosa da casa, e ela cresceu com a partição.** `MiningTable` trata lado ausente por inteiro como neutro (×1), de propósito — sem criatura ativa, o bioma decide sozinho. O efeito colateral é que bioma sem taxa nenhuma não dá erro: `peso_classe × peso_bioma` vira só-classe, a picareta continua entregando minério com outra distribuição, e o sintoma é "a picareta mudou de perfil" meses depois. Enquanto o mundo declarava um bioma só, bastava conferir aquele; com a partição o jogador pisa em **qualquer um do mapa**, então `test_data.gd` cobra taxas de todo bioma **alcançável**. Bioma novo entra com as 11 taxas ou não entra — é o que o documento `mineracao` do bestiário já manda.

- **Campo do bundle é promessa: ou alguém lê, ou ele não viaja.** `creature.biome` e `creature.role` saíam no bundle sem nenhum leitor e com dois terços das linhas nulas; saíram em 2026-08 e continuam no catálogo como anotação editorial. Vale para o caminho inverso também: `map_biomes` só passou a ser exportado quando ganhou consumidor de verdade (a conferência acima) — exportar antes teria só trocado um campo morto por outro.

- **Suíte vermelha significa quebrado; alvo de conteúdo sai como aviso.** `test_data.gd` separa as duas coisas com `_check` e `_warn`, e `pnpm game:export` usa o mesmo par (aborta / avisa-e-escreve). Os dois guardas têm de concordar sobre qual é qual: eles já divergiram, e foi por aí que `CRT-013` saiu num bundle conhecendo um golpe `awakeningOnly` sem ter Despertar — golpe que aparece na ficha e nunca pode ser usado. O export não olhava, e o teste reprovava na cobertura 1:1 (que era meta) sem checar o golpe morto (que era erro). Esse par de critérios existiu até 2026-09, quando o Despertar virou buff universal (toda criatura desperta, não há cobertura a cobrar); a lição — os dois guardas concordarem — continua valendo para peso de spawn (erro) e `mining_rates` de bioma (alvo).
- **Classe é especialização de atributo, não linhagem.** Cada classe do bundle traz `primaryStat` (um dos cinco: `hp`, `attack`, `defense`, `speed`, `charge`) e `primaryStatBonusPct`. `stats_at_level` multiplica **aquele stat e nenhum outro**, depois da curva de nível. O `1.20` não existe em GDScript — é regra 1 aplicada: mudar o bônus das classes é um PATCH no bestiário. Também **não existe matchup CLS×CLS**: o bônus é o mesmo contra qualquer adversário, e nenhum valor lê a classe do oponente. Se aparecer necessidade de uma matriz entre classes, ela não é bem-vinda — é a regra do Changelog 0.01, e o `test_data.gd` a prende em duas frentes.
- **Terminologia travada:** "Despertar Ancestral" é o único termo, e desde 2026-09 ele nomeia um **buff** universal de combate (Ataque e Defesa × `rules.awakening.multiplier` por `rules.awakening.durationTurns` rodadas, golpes `awakeningOnly` liberados, os dois lados despertam — a IA ativa sozinha em `Battle._auto_awaken_enemy`), não uma transformação em outra criatura. *Evolução* e *Forma Ancestral* estão descontinuados e a API do bestiário rejeita com 422.
- Comentário explica **por quê**, não o quê. Os comentários deste repo carregam o histórico das decisões — quando mudar uma, atualize o comentário junto ou ele vira mentira.

## Coisas para NÃO fazer

- Não escrever constante de balanceamento em código (regra 1). O multiplicador de classe é o caso mais recente: `CombatMath.stat_with_class_bonus` recebe o percentual como parâmetro de propósito, e travá-lo em 1.20 passaria em todos os testes de hoje.
- Não presumir classe pela linhagem da criatura. Artrópode não é uma classe, sinapsídeo não é uma classe — o vínculo foi desfeito em 2026-08 e não existe validação taxonômica em lugar nenhum.
- Não editar `data/bestiary.json` (regra 2).
- Não commitar `.glb`, `.png` ou áudio sem LFS — o `.gitattributes` já cobre, mas confira `git lfs status` se um binário aparecer no diff. Blob commitado direto fica no histórico para sempre.
- Não adicionar tabela ao bestiário sem incluí-la em `packages/db/src/tables.ts` lá — o dump sai sem ela em silêncio.
- Não comparar versão (`0.NN`) como texto: `'0.99' > '0.104'` em ordenação lexicográfica.
- Não deixar andaime de screenshot commitado.
- Não escrever `position.y = ...` para apoiar ator no chão. Use `ground_on_spot()` (regra acima).
- Não adicionar classe concreta às listas de `is` do `WorldRoot`/`WorldSelection`. Ponto fixo clicável herda de `InteractableActor` e já é coberto.
- Não aplicar `attack_modifier`/`defense_modifier` fora de `Battle._apply_modifier` — o clamp acumulado tem um dono só.
- Não cadastrar taxa de bioma para minério exclusivo "para completar a cobertura". A ausência É a exclusividade.
- Não abrir tela nova sem escolher a família: ou pausa a árvore e entra em `WorldRoot._modal_open()`, ou não pausa e se guarda sozinha. Tela que não faz nem um nem outro deixa o mapa responder por baixo dela.
- Não criar `TextureRect` de miniatura sem `expand_mode = EXPAND_IGNORE_SIZE`. `custom_minimum_size` sozinho é um piso, não um teto — o padrão (`EXPAND_KEEP_SIZE`) usa o tamanho em pixels da textura como mínimo, e uma arte maior que a miniatura infla o container inteiro (regra acima, card de criatura).

## Onde procurar

- **README.md** — o que existe, como rodar, e o raciocínio de cada sistema
- **ROADMAP.md** — o que está pendente e em que ordem
- **ANIMACOES_PENDENTES.md** — briefing pra quem for autorar animação nova: o vocabulário de clipe completo, a cadeia `attackVariant` → clipe ponta a ponta, onde o clipe físico é carregado, e a decisão aberta (clipe novo compartilhado vs. clipe por criatura)
- **`scripts/data/bestiary_data.gd`** — o contrato com o bundle
- **`scripts/battle/battle.gd`** — a máquina de turnos, e o comentário sobre `replace_active` vs `_do_switch`
- **`scripts/world/interactable_actor.gd`** — a base dos pontos fixos clicáveis e o contrato do apoio no chão
- **`scripts/world/world_root.gd`** — `_modal_open()` e o comentário sobre as duas famílias de tela; é o árbitro de quem recebe input
- **`scripts/world/player_progress.gd`** — o único estado que persiste em disco hoje (Glifos); autoload `Progress`, nunca referenciado pelo identificador global bare fora deste arquivo (mesmo motivo de `Bestiary`)
- **`scripts/world/arena_actor.gd`** / **`portal_guardian_actor.gd`** — arena e guardião do portal (documento `glifos-e-portais` no bestiário). Desde 2026-08 os dois são **dados**: o duelo vem de `duelists[].duel` no bundle e o guardião só é instanciado quando `maps[].connections` traz uma travessia com `requiredGlyph`. Travessia dentro de uma era é livre, então o PZ-01 hoje **não tem guardião** — e isso é o dado mandando, não conteúdo removido. `Glifo concedido vazio` também é estado normal: só a arena do último mapa de uma era concede
- **`scripts/dev/test_data.gd`** — o guarda do contrato; rode depois de todo `game:export`. Espelha os critérios do export: `_check` para erro de dado, `_warn` para meta de conteúdo
- **`scripts/world/player_loadout.gd`** — posse e slots do resto do set; `scripts/ui/crafting_screen.gd` é a bancada que fabrica e `test_equipment.gd` prende as duas asserções negativas (minério glacial não vaza; receita incompleta não cobra)
- **`scripts/dev/test_glyphs.gd`** — `PlayerProgress` isolado + a condição de concessão de Glifo (vitória de arena, nunca vitória selvagem)
- **`scripts/dev/test_palette.gd`** — ciclo de vida da aura do Despertar (nasce, não empilha, morre com a transformação), com a rampa neutra fixa
- **`scripts/dev/test_creature_bodies.gd`** — o contrato de todo corpo de criatura com `modelUrl` (esqueleto da UAL, retarget, loops), a escada de nado e "nenhum clipe anda sozinho" no elenco inteiro; `test_tripo_shell.gd -- --shell <modelUrl>` faz o mesmo para um corpo só, com a altura em jogo
- **`scripts/dev/test_dungeon_bodies.gd`** — o placeholder genérico único (Imp) por retarget na UAL, e o fallback real pelo bundle pra quem não tem modelo
- **`scripts/world/creature_actor.gd`** — resolução de modelo (`model_path`: definitivo → placeholder único → cápsula), montagem do visual e animação; `CompanionActor` consome o mesmo contrato
- **`scripts/world/element_palette.gd`** — desde 2026-09, só presentação: aura do Despertar e efeito de golpe/status (`play_battle_effect`), com rampa neutra fixa — menos o buff, que lê a paleta do elemento no bundle (`element_ramp`). O comentário de topo explica por que a recoloração de corpo por elemento saiu (o elenco convergiu pra um placeholder único, e ela parou de distinguir qualquer coisa) e por que a aura é o efeito de área do BinbunVFX ancorado no chão (`assets/BinbunVFX_Vol2/`) em vez de casca de malha
- **`scripts/world/character_rig.gd`** — humanos por receita (jogador e NPCs); `test_characters.gd` guarda o kit, as receitas do bundle e a fusão das bibliotecas de animação
- **`scripts/world/map_dressing.gd`** — ambiência e props de cenário por mapa (apresentação pura, padrão `WorldPopulator`); o layout do PZ-01 vive aqui
- **`scripts/world/map_terrain.gd`** — relevo com colisão e a consulta `height_at`; planície central plana até 16 m (onde vive o gameplay de chão plano), colinas e rim só na zona externa, e os dois trechos emersos reservados a NPCs/portais — a costa da borda -Z (`on_coast`) e a ilha da arena no miolo (`on_island`), as duas sem bioma e sem spawn. A origem do mapa **não é mais altura zero**: é o topo da ilha, a 2,6 m. Malha, física e consulta saem da mesma grade — nunca discordam
- **`../avyron-bestiary/CLAUDE.md`** — o briefing do bestiário
- **`../avyron-bestiary/docs/DATA_WORKFLOW.md`** — como inserir e corrigir dados
